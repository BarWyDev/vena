---
// /dashboard is retired — the donations view is now the home of the authenticated area.
// Redirect preserves old bookmarks/links. Uses Astro.response (not a top-level return)
// to stay compatible with the astro-eslint-parser + typescript-eslint setup.
Astro.response.status = 302;
Astro.response.headers.set("Location", "/donations");
---
